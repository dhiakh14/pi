/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PredictionResponseDto } from '../../models/prediction-response-dto';
import { ProjectDto } from '../../models/project-dto';

export interface PredictStatus$Params {
      body: ProjectDto
}

export function predictStatus(http: HttpClient, rootUrl: string, params: PredictStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<PredictionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, predictStatus.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PredictionResponseDto>;
    })
  );
}

predictStatus.PATH = '/predict/project_status';
